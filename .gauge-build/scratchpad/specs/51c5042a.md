# PAGE SPEC

- **Filename:** `Thm - Ad is a Smooth Representation and its Differential is ad.md`
- **Type:** theorem
- **Chapter:** Gauge Theory I — Lie Groups, Representations, and Group Actions  (folder `Gauge Theory I/`)
- **Section:** §1.3 Representations of Lie Groups and Lie Algebras

## Spec (from the manifest)

type: theorem; source items: B-T1.3.1, B-T1.3.2, B-T1.4.7, B-D1.3.4 (→ link `Def - Adjoint Representation`), B-E1.3.1, B-E1.3.2, B-E1.3.3, B-D1.3.14 (ad); prereqs: [Def - Adjoint Representation, Def - Representation of a Lie Group, Def - Exponential Map of a Lie Group, Thm - Fundamental Theorem on Flows]; statement: (i) $\operatorname{Ad}\colon G\to GL(\mathfrak g)$, $\operatorname{Ad}_g=d_e\alpha_g$, is a Lie group representation; (ii) for $G\subset GL(n;\mathbb K)$ a matrix group, $\operatorname{Ad}_g X=gXg^{-1}$; (iii) $d_e\operatorname{Ad}=\operatorname{ad}$, i.e. $\tfrac{d}{dt}\big|_0\operatorname{Ad}_{\exp tX}Y=[X,Y]$ for every Lie group; (iv) if $G$ is abelian then $\operatorname{Ad}_g=\operatorname{id}$; proof: (i) source full except smoothness — prove smoothness from smoothness of $(g,h)\mapsto\alpha_g(h)$ by differentiating in the second variable (a smooth map $G\times\mathfrak g\to\mathfrak g$); (ii) source full; (iii) source only for matrix groups — give the general proof: the flow of the left-invariant field $Y$ is $R_{\exp tY}$ (Lemma, from `Thm - One-Parameter Subgroups are the Integral Curves of Left-Invariant Vector Fields`; write both pages so that the exp page precedes this one in reading order or move Lemma here), then $\operatorname{Ad}_{\exp tX}Y=(R_{\exp(-tX)})_*Y|_e$ and differentiate using the Lie derivative definition of the bracket (DG V `Def - Lie Derivative of a Vector Field`); reference: Lee ISM 2e Prop 20.24 and Thm 20.27; (iv) immediate; spec: E1.3.2 (explicit $\operatorname{Ad}_g$ of $SU(2)$ in the Pauli basis, a $2\varphi$-rotation) and E1.3.3 ($\operatorname{Ad}_{-1}=1$, not faithful) are worked as Targets and drilled in the exercise below. NOTE on ordering: place this page after `Thm - One-Parameter Subgroups...` in the chapter's dependency order (it is listed in §1.3 for thematic reasons; its prereqs include the §1.4 exp page, which the topic page must list first in its Notation Registry — acceptable because both are in chapter I; the writer of this page must not be started before the exp page exists).
