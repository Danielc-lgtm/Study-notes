# PAGE SPEC

- **Filename:** `Thm - Removal of Double Points by Handle Attachment.md`
- **Type:** theorem
- **Chapter:** Gauge Theory XIII — Intersection Forms, Four-Manifold Classification, and Donaldson's Theorem  (folder `Gauge Theory XIII/`)
- **Section:** §13.1 Representing Homology Classes by Embedded Surfaces

## Spec (from the manifest)

type: theorem; source items: R5.1.1 (Step 4, with Bär's figures), T5.1.1 (Step 4 part); prereqs: [Def - Transverse Immersion with Double Points, Thm - Tubular Neighbourhood Theorem, Thm - The Inverse Function Theorem, Thm - Bounding Classes Vanish, Def - R-Orientation and the Fundamental Class, Def - Manifold with Boundary and Induced Orientation, Thm - Smooth Orientability Equals Z-Orientability]; statement: Let $f : S \to X$ be an immersion of a closed oriented surface into an oriented four-manifold with exactly $N$ transverse double points. Then there is a closed oriented surface $\tilde S$ of genus $g(S) + N$ and an embedding $\tilde f : \tilde S \to X$ that agrees with $f$ outside $N$ disjoint small balls around the double points, with $\tilde f_*[\tilde S] = f_*[S] \in H_2(X;\mathbb{Z})$; proof: source: sketch with pictures, orientation bookkeeping only indicated; the page proves: (Lemma 1, local model) at a double point $x_i$ there is a chart $\Phi : U(x_i) \to \mathbb{R}^4 = \mathbb{C}^2$ with $\Phi(x_i) = 0$, $\Phi(f(U(p_i))) = \mathbb{R}^2 \times \{0\}$, $\Phi(f(U(q_i))) = \{0\} \times \mathbb{R}^2$ — construct it: choose any chart $\Phi_0$ sending the two tangent planes to the coordinate planes; near $0$ the two sheets are graphs $\{(u, g_1(u))\}$ and $\{(g_2(v), v)\}$ with $dg_j(0) = 0$; the map $(u, v) \mapsto (u + g_2(v), v + g_1(u))$ is a local diffeomorphism by the inverse function theorem and its inverse straightens both sheets simultaneously (verified: it maps $(u, 0) \mapsto (u, g_1(u))$ and $(0, v) \mapsto (g_2(v), v)$); (Lemma 2, the band) the annulus $Z := \{(\cos\theta \, e^{i\alpha}, \sin\theta \, e^{i\epsilon\alpha}) \mid \theta \in [0, \pi/2], \alpha \in \mathbb{R}/2\pi\mathbb{Z}\} \subset S^3$, $\epsilon \in \{+1, -1\}$, is an embedded annulus in $S^3 \subset \mathbb{C}^2$ with boundary the two Hopf circles $S^1_1 = (\mathbb{C} \times 0) \cap S^3$ and $S^1_2 = (0 \times \mathbb{C}) \cap S^3$, and it is disjoint from the two planes $\mathbb{R}^2 \times 0$, $0 \times \mathbb{R}^2$ except along its boundary (verified from the formula); the choice of $\epsilon$ is dictated by orientations: (Lemma 3, orientations) removing the discs $D_{p_i} = f^{-1}(\Phi^{-1}(B(0,1))) \cap U(p_i)$ and $D_{q_i}$ from $S$ and gluing $Z$ along the boundary circles gives an oriented surface iff the orientation of $Z$ (induced from a chosen orientation of the annulus) restricts to the boundary circles as the orientations induced from $S \setminus (D_{p_i} \cup D_{q_i})$; for exactly one choice of $\epsilon$ this holds (computation: the boundary orientations of $S^1_1$ and $S^1_2$ induced from $Z$ are $(\pm)$ the standard ones depending on $\epsilon$, while those induced from $S$ are determined by the orientations of the two sheets, whose relation to the standard ones is the sign $\varepsilon(x_i)$ of the double point; the page tabulates the four cases) — this is the orientation bookkeeping Bär only indicates; (Lemma 4, the new map) $\tilde f$ := $f$ on $S \setminus (D_{p_i} \cup D_{q_i})$ and $\Phi^{-1}$ on $Z$ (rescaled to radius $1$), an embedding because $Z$ meets the planes only along the boundary circles; $\tilde S = S \# T^2$ (genus count via Euler characteristic: $\chi(\tilde S) = \chi(S) - 2 + 0 = \chi(S) - 2$, from the Mayer–Vietoris additivity on the page or the exercise); iterate over the $N$ double points with disjoint balls; (Lemma 5, homology class unchanged) Bär's route: construct a compact oriented $3$-manifold $W$ with $\partial W = S \sqcup \overline{\tilde S}$ and a continuous $F : W \to X$ with $F|_S = f$, $F|_{\tilde S} = \tilde f$, namely $W := \big(S \setminus \bigcup_i (D_{p_i} \sqcup D_{q_i})\big) \times [0,1] \cup \bigcup_i H_i$, where $H_i := \{ \rho z \mid z \in Z, \rho \in [0,1] \} \subset \bar B(0,1) \subset \mathbb{C}^2$ is the cone on the band $Z$ (a solid handle whose boundary is $Z$ together with the two cones on the Hopf circles, i.e. the two flat discs $(\mathbb{R}^2 \times 0) \cap \bar B$ and $(0 \times \mathbb{R}^2) \cap \bar B$), glued to $\partial D \times [0,1]$ along the radial segments of the flat discs, with $F := f \circ \mathrm{pr}_1$ on the product part and $F := \Phi^{-1}$ on $H_i$ (the cone point maps to $x_i$: Bär's "nerve of the handle"); the page verifies that $W$ is a compact topological $3$-manifold with collared boundary $S \sqcup \overline{\tilde S}$ (the collar coming from the product structure and the radial coordinate $\rho$), oriented compatibly, and that $F$ is continuous; then `Thm - Bounding Classes Vanish` (iii) gives $f_*[S] = \tilde f_*[\tilde S]$. The writer must produce an explicit, coordinatised construction of $W$ as a compact smooth (or at least topological, collared) oriented $3$-manifold with the stated boundary; an acceptable alternative is the following cleaner route, which the page may use instead of $W$: $f_*[S] - \tilde f_*[\tilde S]$ is the image under $H_2(\bigsqcup_i \bar B_i) \to H_2(X)$ of a class, because the two cycles agree outside the balls $B_i$ — made precise by triangulating $S$ and $\tilde S$ compatibly (the difference $f_*(c_S) - \tilde f_*(c_{\tilde S})$ of the fundamental cycles of `Thm - Triangulations Represent the Fundamental Class` is a cycle supported in $\bigsqcup \bar B_i$), and $H_2(\bar B_i;\mathbb{Z}) = 0$. The page chooses ONE route and proves it fully; the manifest recommends the second (support) route as the primary and Bär's cobordism $W$ as a remark. Reference: Bär pp. 131–132 (figures); Gompf–Stipsicz §1.2 (the "resolving double points" picture); Kirby, *The Topology of 4-Manifolds*, Ch. I. spec: Why-true: "a transverse double point looks like two coordinate planes, and the two circles where the planes meet the unit sphere are the two ends of a band; trading the two discs for the band removes the intersection and adds a handle"; Targets: the representability theorem.
