# PAGE SPEC

- **Filename:** `Thm - A Covariant Derivative Determines a Connection on the Frame Bundle.md`
- **Type:** theorem
- **Chapter:** Gauge Theory IV — Connections and Curvature on Principal Bundles  (folder `Gauge Theory IV/`)
- **Section:** §4.4 Connections on Associated Bundles and Induced Covariant Derivatives

## Spec (from the manifest)

type: theorem; source items: A-T2.2.5(i) (Steps 1–3), A-X2.2.8 (Step 1, as Lemma), B-T2.3.2, B-T2.3.3, B-E2.3.1, B-E2.3.2; prereqs: [Def - Connection on a Vector Bundle, Def - Connection Matrix and Local Form of a Connection, Def - Frame Bundle of a Vector Bundle, Def - Connection on a Principal Bundle, Def - Local Connection Form and Gauge Potential, Thm - Transformation of Local Connection and Curvature Forms, Thm - Gauge Transformation Law for Connection 1-Forms]; statement: Let $E\to M$ be a vector bundle with frame bundle $\operatorname{Fr}(E)$. (a) For every connection $\nabla$ on $E$ there is a unique connection $\omega$ on $\operatorname{Fr}(E)$ with $e^*\omega=A(\nabla,e)$ for every local frame $e$; (b) equivalently (B's construction), for $X\in T_p\operatorname{Fr}(E)$ and any curve $p(t)$ of frames with $\dot p(0)=X$, $\omega(X)$ is the matrix defined by $\tfrac{\nabla}{dt}\big|_0p_j=(p(0)\cdot\omega(X))_j$, and this is well defined; (c) in the coordinate section $s=(\partial_1,\dots,\partial_n)$ of $\operatorname{Fr}(TM)$, $(s^*\omega(\partial_k))^j{}_i=\Gamma^j_{ik}$; (d) $\nabla\mapsto\omega$ is a bijection $\mathcal A(E)\to\mathcal A(\operatorname{Fr}(E))$ (with inverse the induced connection of the next theorem); proof: A full in Steps 1–3 with Step 1 left as exercise — write Lemma 1 ($d\hat R_{(p,g)}(v,w)=dR_g(v)+(\theta_g(w))_P(pg)$, the differential of the action map, by the product rule on $\hat R(p(t),g(t))$), Lemma 2 (over a trivialising $U$, for any $A\in\Omega^1(U;\mathfrak{gl}_k)$ there is a unique connection on $\operatorname{Fr}(E)|_U$ with $e^*\omega=A$ — the reconstruction formula, with the equivariance check using chapter I's $(R_g)_*\xi_P=(\operatorname{Ad}_{g^{-1}}\xi)_P$), Step 3 (independence of the frame, via Lemma 1 and (14), which is exactly the local transformation law (a) of the previous theorem); (b) B's argument with the three checks (curve independence via the covariant derivative along a curve — link RG I `Def - Covariant Derivative along a Curve`; property (2) with $p(t)=p\exp(tX)$; property (1)); (c) unwinding; (d) after the next theorem; reference: Kobayashi–Nomizu I Ch. III §1 (Prop 1.? "linear connections"), Baum Satz 3.? ; spec: Convention callout: A writes $e^*a=A(\nabla,e)$; RG I's Cartan connection forms $\omega^a{}_b$ are the entries of $s^*\omega$.
