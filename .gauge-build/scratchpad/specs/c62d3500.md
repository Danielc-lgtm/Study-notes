# PAGE SPEC

- **Filename:** `Def - Frame Bundle of a Vector Bundle.md`
- **Type:** definition
- **Chapter:** Gauge Theory III — Fibre Bundles, Principal Bundles, and Associated Bundles  (folder `Gauge Theory III/`)
- **Section:** §3.2 Principal Bundles and the Frame Bundle

## Spec (from the manifest)

**[anchor]** — type: definition (compound); source items: A-D2.2.2, A-R2.2.1, B-D2.2.2, B-T2.2.3, B-E2.2.2, B-E2.2.3, B-E2.2.4 (table); prereqs: [Def - Principal G-Bundle, Def - Vector Bundle, Def - Local Frame]; spec: $\operatorname{Fr}(E)=\bigsqcup_m\operatorname{Fr}(E_m)$, frames as isomorphisms $\mathbb R^k\to E_m$, right $GL_k(\mathbb R)$-action $e\cdot h$, charts $\Psi_U(m,h)=e(m)h$ (24) with transition $(m,h)\mapsto(m,g(m)h)$ — the full verification that these charts define a Hausdorff second-countable smooth structure and that all four principal-bundle clauses hold (R2.2.1's "clear from the construction" written out; B-T2.2.3's local triviality written out); the orthonormal frame bundle $O(E)$ of a Euclidean bundle and $U(E)$ of a Hermitian bundle (smooth structure via Gram–Schmidt — deferred to the reductions theorem); the table of structure groups $GL(n;\mathbb K)$, $O(n)$, $U(n)$, $GL^+(n;\mathbb R)$, $SO(n)$ for bases respecting extra structure (E2.2.4; each row verified after the reductions theorem). Non-example: the set of all bases of $TM$ with the *left* action $h\cdot e$ is not a right principal bundle without the inverse. Calibration: $\operatorname{Fr}(M\times\mathbb R^k)=M\times GL_k$; $\operatorname{Fr}(E)$ has a global section iff $E$ is trivial (via the next theorem).
