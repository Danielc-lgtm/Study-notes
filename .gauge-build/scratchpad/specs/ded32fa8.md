# PAGE SPEC

- **Filename:** `Thm - Principal Connections Induce Covariant Derivatives on Associated Bundles.md`
- **Type:** theorem
- **Chapter:** Gauge Theory IV — Connections and Curvature on Principal Bundles  (folder `Gauge Theory IV/`)
- **Section:** §4.4 Connections on Associated Bundles and Induced Covariant Derivatives

## Spec (from the manifest)

**[anchor]** — type: theorem; source items: A-T2.2.5(ii) (Step 4), A-D2.2.14, A-R2.2.6, A-T2.2.6, A-T2.2.7, A-X2.2.7 (identification clause), B-D2.3.5, B-T2.3.10, B-R2.3.9; prereqs: [Def - Connection on a Principal Bundle, Def - Associated Bundle, Thm - Sections of an Associated Bundle are Equivariant Functions, Thm - Bundle-Valued Forms on the Base are Basic Equivariant Forms on the Total Space, Def - Representation of a Lie Algebra, Def - Connection on a Vector Bundle, Def - Curvature of a Vector-Bundle Connection, Def - Curvature of a Principal Connection, Thm - Structure Equation for the Curvature, Def - Induced Connections on Dual, Hom, and Endomorphism Bundles]; statement: Let $\omega$ be a connection on $P$ and $\rho\colon G\to GL(V)$ a representation with differential $\rho_*$. (a) There is a unique connection $\nabla=\nabla^\omega$ on $E=P\times_\rho V$ such that, for every section $s$ with equivariant lift $\hat s$, $\pi^*(\nabla s)$ corresponds to the basic equivariant $V$-valued 1-form $d\hat s+\rho_*(\omega)\hat s$ (A's (47)). (b) Locally, for a section $p$ of $P$ over $U$ and $v\colon U\to V$, $\nabla_X[p,v]=[p,\partial_Xv+\rho_*(p^*\omega(X))v]$ (B's formula), and this is independent of the representative (B-T2.3.10 completed: the computation with $(pg)^*\omega=\operatorname{Ad}_{g^{-1}}p^*\omega+g^*\theta$ and $\rho_*(g^*\theta(X))\rho(g^{-1})v=\partial_X(\rho(g^{-1})v)$... written in full). (c) The curvature of $\nabla^\omega$ is $\rho_*(F_\omega)\in\Omega^2(M;\operatorname{End}E)$, i.e. $\pi^*F_{\nabla^\omega}$ corresponds to $\rho_*(\Omega)$; locally $F_{\nabla^\omega}=\rho_*(F_\alpha)$ (A-T2.2.7). (d) For $E$ a vector bundle with frame bundle $\operatorname{Fr}(E)$ and $\rho$ the standard representation, $\nabla^\omega$ has connection matrices $e^*\omega$, so the constructions of this and the previous theorem are mutually inverse; the induced connections on $E^*$, $\operatorname{End}E$, $\Lambda^pE$ (via the corresponding representations) coincide with II's direct constructions (A-T2.2.6, A-X2.2.7). (e) For $\rho=\operatorname{Ad}$ on $\operatorname{ad}P$, $\nabla^\omega$ is the connection $\nabla_a$ of A's Bianchi identity; proof: A full for (a) modulo equivariance of $d\hat s+a\cdot\hat s$ and uniqueness — write: $(d\hat s+\rho_*(\omega)\hat s)(\xi_P)=-\rho_*(\xi)\hat s+\rho_*(\xi)\hat s=0$ (differentiate $\hat s(p\exp t\xi)=\rho(\exp(-t\xi))\hat s(p)$), equivariance by $R_g^*d\hat s=d(\rho(g^{-1})\hat s)$ and $R_g^*\omega=\operatorname{Ad}_{g^{-1}}\omega$ with $\rho_*\circ\operatorname{Ad}_{g^{-1}}=\rho(g^{-1})\rho_*\rho(g)$ (naturality of $\rho_*$, chapter I); Leibniz for $f\hat s$; uniqueness since $\nabla s$ is determined; (b) B's computation completed; (c) apply $d^\nabla$ twice to (47) and use the structure equation and $\rho_*[\xi,\eta]=[\rho_*\xi,\rho_*\eta]$; (d),(e) unwinding; reference: Kobayashi–Nomizu I Ch. III Prop 1.?; Baum Satz 3.? ; spec: Mechanism summary: "the covariant derivative of a section is the ordinary derivative of its equivariant lift corrected by the connection form so as to become horizontal." Targets: Levi-Civita as an $O(n)$-connection on the orthonormal frame bundle (RG I bridge), spinor connections (VIII), the Yang–Mills coupling to matter (VII).
