---
type: definition
subject: probability-geometry
prereqs:
  - "Def - First Homology, Characters, and Finite Fourier Analysis"
  - "Thm - Mass of a Free Homotopy Class"
tags: [paper, brownian-loops, homology]
source: "Brownian Loops — Homotopy and Homology"
paper-ref: "Definition 6.1"
---

# Notation

- $X = \Gamma\backslash\mathbb{H}^2$ a geometrically finite hyperbolic surface (genus $g$, $b = n_C + n_F$ ends).
- $\pi_1(X) \cong \Gamma$ the fundamental group; $H_1(X, \mathbb{Z})$ the first (integer) homology group; $[\cdot] : \Gamma \twoheadrightarrow H_1(X, \mathbb{Z})$ the Hurewicz abelianisation map, sending $\gamma \mapsto [\gamma]$. Rank: $r = 2g$ if $X$ is closed, $r = 2g + b - 1$ if $X$ has $b \ge 1$ ends.
- $\mathcal P_X$ the set of oriented primitive closed geodesics; $\ell_\gamma > 0$ the length of $\gamma \in \mathcal P_X$; $m \ge 1$ the winding count.
- $\beta \in H_1(X, \mathbb{Z}) \cong \mathbb{Z}^r$ a homology class.
- $\kappa \ge -\frac14$ the killing parameter; $s := \frac12 + \sqrt{\frac14 + \kappa}$ the spectral parameter (assumed $\operatorname{Re}s > \delta$).
- $\delta$ the critical exponent of $\Gamma$ (equivalently, the Hausdorff dimension of $\Gamma$'s limit set).
- $C_X(\gamma^m)$ the free homotopy class winding $m$ times around $\gamma$; $\mu^\kappa_X(C_X(\gamma^m))$ its killed loop-measure mass.

> [!recall]- First homology $H_1(X, \mathbb{Z})$ and the Hurewicz map $[\gamma]$
> **Formally:** the *first homology group* of $X$ (with integer coefficients) is the abelianisation of the fundamental group: $H_1(X, \mathbb{Z}) := \Gamma / [\Gamma, \Gamma]$, where $[\Gamma, \Gamma]$ is the *commutator subgroup* (the subgroup generated by all elements of the form $q h q^{-1} h^{-1}$). The *Hurewicz map* is the quotient homomorphism $\Gamma \twoheadrightarrow H_1(X, \mathbb{Z})$, $\gamma \mapsto [\gamma]$. It is a group homomorphism, so $[\gamma^m] = m[\gamma]$. As a group, $H_1(X, \mathbb{Z}) \cong \mathbb{Z}^r$ with $r = 2g$ for closed genus-$g$ and $r = 2g + b - 1$ for genus $g$ with $b \ge 1$ ends.
> **In words:** homotopy records "which holes you went around, in which order, forwards or backwards"; *homology* forgets everything about the order and remembers only the net winding around each independent cycle. Two loops that go once around the same handle end up in the same homology class no matter what other manoeuvres they do in between, provided the net winding on every other handle is zero. The Hurewicz map is the operation that performs this forgetting: it sends the non-commutative product on $\Gamma$ to an ordinary integer-vector sum in $\mathbb{Z}^r$.
> **Concretely:** on a torus $T^2 = \mathbb{R}^2/\mathbb{Z}^2$ (genus $1$, closed), $\Gamma = \mathbb{Z}^2$ is already abelian, so $[\Gamma, \Gamma] = 0$ and $H_1 = \mathbb{Z}^2$: a class is an integer pair $(a, b) = ($net horizontal winding, net vertical winding$)$; the Hurewicz map is the identity. On a genus-$2$ closed surface, $\Gamma$ has $4$ generators $\alpha_1, \beta_1, \alpha_2, \beta_2$ with the relation $[\alpha_1,\beta_1][\alpha_2,\beta_2] = 1$ — the two commutators (order-of-traversal information) — and abelianising kills these commutators to give $H_1 = \mathbb{Z}^4$. On a once-punctured torus ($g=1$, $b=1$), $r = 2\cdot 1 + 1 - 1 = 2$, so $H_1 = \mathbb{Z}^2$ still. Full detail: [[Def - First Homology, Characters, and Finite Fourier Analysis]].

> [!recall]- Killed loop-measure mass $\mu^\kappa_X(C_X(\gamma^m))$
> **Formally:** the mass, under the killed Brownian loop measure with killing rate $\kappa \ge -\frac14$, of the free homotopy class $C_X(\gamma^m)$ is
> $$\mu^\kappa_X(C_X(\gamma^m)) = \frac{1}{m}\cdot\frac{e^{(1-s)m\ell_\gamma}}{e^{m\ell_\gamma}-1},\qquad s = \frac12 + \sqrt{\frac14 + \kappa},$$
> a positive real number (§3.1.2, from Theorem 3.5 with $\phi(\lambda) = \lambda + \kappa$).
> **In words:** a single weight assigned to each free-homotopy loop class; long classes are exponentially penalised. The sum of all these weights across every $(\gamma, m)$ is finite (Corollary 4.3) and equals $-\log Z_X(s)$. In this note we regroup those weights not by free-homotopy class but by their common homology class — an infinite-to-one collapse, but one whose partial sums still converge.
> **Concretely:** on an infinite-area surface with one primitive geodesic of length $\ell = 1$ and $\kappa = 0$ (so $s = 1$), $\mu(C_X(\gamma^m)) = \frac{1}{m(e^m - 1)}$: about $0.582$ for $m=1$, $0.078$ for $m=2$, $0.017$ for $m=3$; the total mass over all $m$ is about $0.68$. Full derivation: [[Thm - Mass of a Subordinate Brownian Loop Class]].

---

# Statement

> **Definition 6.1 (mass in a homology class; Belyaev–Huseynli 6.1).** For $\beta \in H_1(X, \mathbb{Z})$ and $s = \frac12 + \sqrt{\frac14 + \kappa}$ with $\operatorname{Re}s > \delta$,
> $$\mu^\kappa_X(\beta) := \sum_{\substack{\gamma \in \mathcal P_X,\ m \ge 1\\ m[\gamma] = \beta}}\mu^\kappa_X(C_X(\gamma^m)) = \sum_{\substack{\gamma \in \mathcal P_X,\ m \ge 1\\ m[\gamma] = \beta}}\frac{1}{m}\cdot\frac{e^{(1-s)m\ell_\gamma}}{e^{m\ell_\gamma} - 1}.$$
> The mass of the killed Brownian loop measure grouped by homology class $\beta$ is the sum of the free-homotopy-class masses over all pairs $(\gamma, m)$ whose iterate $\gamma^m$ lies in the class $\beta$.

---

# In One Line

The killed loop mass grouped by *homology* (net winding around each cycle) rather than *homotopy* (order of traversal remembered): each homology class $\beta$ inherits mass from the infinitely many free-homotopy classes lying above it.

---

# Motivation and Unpacking

**Why homology, having already done homotopy.** The homotopy grading of §3 is fine — every "which handle in which order" distinction is preserved — but it is *too* fine for intersection-theoretic questions. Two loops' *geometric* intersection number is a homotopy invariant (it depends on the actual sliding equivalence class of the loops), but their *algebraic* intersection number — the signed count of crossings, adding $+1$ for one orientation and $-1$ for the other — is homology-invariant. Since algebraic intersections are homology data and geometric intersections are homotopy data, the homology-graded measure is the natural object for questions like "what is the expected algebraic self-intersection of a random loop?".

**The many-to-one collapse is infinite.** For a surface of genus $g \ge 2$, each single homology class $\beta \in H_1 \cong \mathbb{Z}^{2g}$ is the image of *infinitely many* distinct conjugacy classes in $\Gamma$. The homotopy classes with $[\gamma^m] = \beta$ are unbounded in length — one can insert an arbitrarily long commutator into the loop without changing its net winding — but the exponential suppression $e^{(1-s)m\ell_\gamma}/(e^{m\ell_\gamma} - 1)$ makes the sum converge whenever $\operatorname{Re}s > \delta$, exactly as in the total mass sum (Corollary 4.3). So $\mu^\kappa_X(\beta)$ is a well-defined finite positive number.

**Concretely.** On a genus-$2$ closed surface with $H_1 = \mathbb{Z}^4$, fix the homology class $\beta = (1, 0, 0, 0)$ (net once around the first meridian, zero elsewhere). This class contains free-homotopy classes like: the primitive loop that goes once around the first meridian and back; that same loop with a commutator inserted (goes once around a handle and unwinds); the same with two nested commutators; etc. Each such conjugacy class is a distinct free-homotopy class of some length, and $\mu^\kappa_X((1,0,0,0))$ sums the mass over all of them. In the special case of the flat torus $T^2$ (where $\Gamma$ is already abelian), each homology class is a *single* free-homotopy class, so $\mu^\kappa_X(\beta) = \mu^\kappa_X(C_X(\gamma_\beta^{m_\beta}))$ with $\beta = m_\beta[\gamma_\beta]$ — the collapse is trivial.

**Why the definition is stated with the sum written twice.** The two right-hand sides of the formula are literally the same thing: the first names the summand as the free-homotopy mass $\mu^\kappa_X(C_X(\gamma^m))$, and the second substitutes its closed-form value $\frac{1}{m}\cdot\frac{e^{(1-s)m\ell_\gamma}}{e^{m\ell_\gamma}-1}$. Writing it both ways makes clear that the *definition* is measure-theoretic (a partial sum of the free-homotopy masses) and the *computation* rides on the §3.1.2 closed form.

**Detection by the Selberg $L$-function.** The paper never computes $\mu^\kappa_X(\beta)$ directly by enumerating conjugacy classes above $\beta$; that would be intractable. Instead, it defines a *character-twisted* Selberg zeta — the Selberg $L$-function ([[Def - Selberg L-Function]]) $L_X(s, \chi)$ — whose logarithm's Fourier coefficients over the character torus $\widehat{H_1(X, \mathbb{Z})}$ are exactly the $\mu^\kappa_X(\beta)$. Fourier inversion ([[Thm - Fourier Inversion by Homology Class|Theorem 6.5]]) then recovers $\mu^\kappa_X(\beta)$ as a single integral over the character torus, bypassing the enumeration.

---

# Where the paper uses this

Introduced in [[Paper - Brownian Loops — Homotopy and Homology — §6 A Probability Measure on Classes|§6.2]] as Definition 6.1; recovered by Fourier inversion in [[Thm - Fourier Inversion by Homology Class|Theorem 6.5]] as the character-torus integral of $-\log L_X(s, \chi)$; enters [[Prop - Total Homology of the Loop Soup|Proposition 6.7]] via the same character-torus inversion for the total-homology distribution of the loop soup. The provenance and intersection-theoretic motivation are recorded in [[Remark - Provenance of the Homology Class Definition|Remark 6.2]].
